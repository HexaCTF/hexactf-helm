apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "challenge-server.fullname" . }}-test-connection"
  labels:
    {{- include "challenge-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "challenge-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
