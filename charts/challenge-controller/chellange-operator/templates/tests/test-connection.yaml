apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chellange-operator.fullname" . }}-test-connection"
  labels:
    {{- include "chellange-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chellange-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
